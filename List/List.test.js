/* eslint-disable no-undef */
// import List from './List.js' // TODO this item my not import scss: Mocha ERR_UNKNOWN_FILE_EXTENSION import css
describe('List',() => {
	describe('constructor',() => {
		const vals = [1,2,3]
		// const list = new List({inner:{vals:vals}})
		// const el = create_dom(domContent)
		// const createdEl = new Html({parent:{el},html:'div',val:'citrone'})
		// createdEl.change({node:{el:createdEl},val:'cherry'})
		// it('changed val',() => {
		//     expect(Html.getChildsFirstVal(el,'div')).to.eql('cherry')
		// })
	})
})
